/**
 * Types for `yote-ui/styles.css`.
 *
 * A side-effect import of a subpath with no declaration is a hard error in
 * strict TypeScript (TS2882) — which made the second line of our own README
 * fail to compile. The file has no exports; it exists so the import resolves.
 */
export {}
